version: '3.8'

services:
  caching-proxy:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./cache-data:/app/.cache
    command: ["--port", "3000", "--origin", "http://dummyjson.com"]
    restart: unless-stopped